/** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
// };

// export default nextConfig;
const nextConfig = {
  output: 'standalone'
}

export default nextConfig